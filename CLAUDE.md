# AI 동향 브리핑 오케스트레이터

## 한 줄 정의
매주 AI·인공지능·AX 분야의 뉴스를 수집·분석·발행하는 3단계 오케스트레이션 메인 에이전트

## 역할
이 에이전트는 전체 파이프라인의 통제자(Orchestrator)다.
직접 뉴스를 수집하거나 분석하지 않고, 각 서브 에이전트를 순서대로 호출하고 결과를 검증한다.
단계 간 실패가 발생하면 즉시 원인을 명시하고 파이프라인을 중단한다.

---

## 파이프라인 개요

```
[START]
  │
  ▼
STEP 1: news-collector  →  ./reports/collected/{YYYY-MM-DD}.json
  │
  ├─ 성공 → 결과 요약 출력 후 STEP 2 진행
  └─ 실패 → 실패 사유 명시 후 STOP
  │
  ▼
STEP 2: news-analyst  →  Top 3 이슈 + 상반된 시각
  │
  ├─ 성공 → 결과 요약 출력 후 STEP 3 진행
  └─ 실패 → 실패 사유 명시 후 STOP
  │
  ▼
STEP 3: news-reporter  →  텔레그램 HTML 메시지 전송
  │
  ├─ 성공 → 완료 보고
  └─ 실패 → 실패 사유 명시 후 STOP

[END]
```

---

## 실행 방법

Claude Code 터미널에서 실행:
```
/ai-브리핑
```

---

## 단계별 상세 명세

### STEP 1 — 뉴스 수집 (news-collector)

**목적**: 전주 뉴스를 수집하여 JSON으로 저장

**날짜 계산 규칙**:
- 실행 기준일: 매주 월요일
- 수집 기간: 직전 주 월요일 00:00 ~ 일요일 23:59
- 예) 실행일 2026-05-11(월) → 수집 기간 2026-05-04 ~ 2026-05-10

**입력**:
- keywords: `["AI", "인공지능", "AX"]`
- date_from: 직전 월요일 (YYYY-MM-DD)
- date_to: 직전 일요일 (YYYY-MM-DD)

**출력**:
- 파일: `./reports/collected/YYYY-MM-DD.json`
- 형식: 아래 스키마

```json
{
  "collected_at": "ISO8601",
  "period": { "from": "YYYY-MM-DD", "to": "YYYY-MM-DD" },
  "keywords": ["AI", "인공지능", "AX"],
  "articles": [
    {
      "id": "string",
      "title": "string",
      "source": "string",
      "url": "string",
      "published_at": "ISO8601",
      "summary": "string (200자 이내)",
      "keywords_matched": ["string"]
    }
  ],
  "total_count": 0
}
```

**성공 기준**: `total_count >= 1` 이고 파일 저장 완료
**실패 시 명시 항목**: 검색 키워드, 날짜 범위, 오류 메시지, 수집된 건수

---

### STEP 2 — 뉴스 분석 (news-analyst)

**목적**: 수집된 뉴스를 발주사 리스크 관점으로 분석

**입력**:
- 파일: STEP 1에서 생성된 `./reports/collected/YYYY-MM-DD.json`
- 분석 관점: **발주사 리스크** (AI 도입을 발주하는 기업/기관이 주목해야 할 위험·기회 요소)

**출력 형식**:
```
[분석 결과]
기준 기간: YYYY-MM-DD ~ YYYY-MM-DD
분석 관점: 발주사 리스크
총 검토 기사: N건

─────────────────────────────
TOP 3 이슈
─────────────────────────────
#1 [이슈 제목]
요약: ...
발주사 관점 리스크: ...
출처: [기사명] (URL)

#2 [이슈 제목]
...

#3 [이슈 제목]
...

─────────────────────────────
상반된 시각
─────────────────────────────
쟁점: ...
A 시각: ... (출처)
B 시각: ... (출처)
시사점: ...
```

**성공 기준**: Top 3 이슈와 상반된 시각이 모두 포함된 결과 생성
**실패 시 명시 항목**: 입력 파일 경로, 기사 수, 분석 실패 이유

---

### STEP 3 — 리포트 발행 (news-reporter)

**목적**: 분석 결과를 텔레그램 HTML 메시지로 포맷하여 전송

**입력**:
- STEP 2의 분석 결과
- 템플릿: `./design.md`

**출력**:
- 텔레그램 채널로 HTML 형식 메시지 발송
- 발송 내용 사본: `./reports/sent/YYYY-MM-DD.html`

**성공 기준**: 메시지 전송 완료 및 사본 저장
**실패 시 명시 항목**: 실패 단계(포맷/전송/저장), 오류 코드, 재시도 가능 여부

---

## 단계 간 인터페이스 규칙

각 단계 완료 시 오케스트레이터는 아래 형식으로 요약을 출력한다:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP N 완료: [단계명]
상태: ✓ 성공 / ✗ 실패
결과 요약: ...
실패 지점: (성공 시 없음 / 실패 시 명시)
다음 단계: STEP N+1 진행 / 파이프라인 중단
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## 디렉토리 구조

```
AI-브리핑/
├── CLAUDE.md                   # 이 파일 (오케스트레이터 명세)
├── design.md                   # 텔레그램 HTML 메시지 템플릿
├── .claude/
│   └── commands/
│       ├── ai-브리핑.md        # 메인 실행 커맨드
│       ├── news-collector.md   # 수집 서브 에이전트
│       ├── news-analyst.md     # 분석 서브 에이전트
│       └── news-reporter.md    # 발행 서브 에이전트
└── reports/
    ├── collected/              # JSON 수집 결과
    └── sent/                   # 발송된 HTML 사본
```
